# packages/shared-schema

Общие контракты Этапа 1 для MyWave Event App.

- `stage1.schema.json` — Role, EventStatus, Event, AuditEvent, ConsentRecord.
- Источник истины реализации: `services/api` models/schemas.
- При изменении API обновить этот файл и `docs/API/API_CONTRACT.md` в одной транзакции.
